Add = lambda No1,No2 : No1 + No2  #Body of function starts with : gets copied from lambda expression to caller  // interpreter never see thid line

Ret = Add(10,11)    # Ret = 10 + 11    //inline function  ( Service comes to home) .pyc file comes with this line. 
print("Addition is : ",Ret)